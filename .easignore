# What EAS uploads to the build server
# If this file is absent, EAS falls back to .gitignore

# VCS metadata
.git
.gitignore
.gitattributes
.github

# Dependencies (EAS installs them on the builder)
node_modules
.pnpm-store
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Caches / outputs
.expo
.expo-shared
.metro-cache
web-build
dist
build
out
lib
tmp
temp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Platform build artifacts
android/.gradle
android/.cxx
android/build
android/app/build
ios/build
ios/DerivedData
ios/Pods

# Credentials (should be handled by EAS Credentials)
*.keystore
*.jks

# IDE / OS
.vscode
.idea
*.iml
.DS_Store
Thumbs.db
desktop.ini

